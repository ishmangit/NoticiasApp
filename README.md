### Instalación

 Instalación de **Ionic**

 `npm install -g ionic`

Instalación del resto de dependencias

```npm install```

### Ejecutar
Lanzar aplicación

```ionic serve -o```

Lanzar página de documentación (compodoc)

```npm run compodoc```